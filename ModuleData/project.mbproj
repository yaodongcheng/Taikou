<base type="solution">
	<!-- 🔴 2026-09-08 实机两连崩修复：本文件只挂 soln_soundtrack（主菜单音乐）。
	     其余模板行（physics_materials / action_sets / collision_infos / skins …）为拷贝死档——
	     引擎按 soln id 跨模块合并（GetMergedXmlForNative），Taikou 的 action_sets.xml 与 Native 同源冲突
	     = KeyNotFoundException（CreateProcessedActionSetsXMLForNative，实崩）。SubModule.xml 的
	     19 个 TaikouCampaign 段才是数据包正路。将来需启用某行：先独立实机验证（启用一个为一个）。 -->
	<file id="soln_soundtrack" name="music/soundtrack.xml" type="soundtrack"/>
	<!-- 🔴 2026-09-14 启用第二行：soln_skins → ModuleData/skins.xml（织田信长专用 race）。
	     为什么必须开这一行：skins.xml 属于 project.mbproj 的 soln 体系，不在 SubModule 的 XmlNode 体系里
	     （Native 自己就是 `<file id="soln_skins" name="ModuleData/skins.xml" type="skin"/>`）。
	     为什么这次不会重演 09-08 那次崩：那次是 Taikou 的 action_sets.xml **逐字拷贝** Native → 同 soln id
	     合并出重复定义 → KeyNotFoundException。本次 skins.xml 里**只多一个 <race id="lwn_nobunaga">**，
	     与 Native 的 race="human" 不同 id → 合并是纯追加，不冲突。
	     回退方法：删掉下面这一行即可（其余改动都是纯数据、可留可删）。 -->
	<file id="soln_skins" name="ModuleData/skins.xml" type="skin"/>
	<!-- 🔴 2026-09-16 启用第三行：soln_module_sound → ModuleData/module_sounds.xml（火器音效要用）。
	     为什么必须开：音效与 skins 同属 project.mbproj 的 soln 体系，**不在 SubModule 的 XmlNode 体系里**——
	     不开这行 = 本文件的 48 个音效（40 个 BGM + 8 组火器）**一个都不注册**，
	     `SoundEvent.GetEventIdFromString` 全查不到。实机症状：铁炮开火只有引擎默认的弩声
	     （LWN 日志会打 `[FirearmFx] 音效 'tk_teppo_fire_near' 查不到 —— 该档静音`）。
	     为什么不会重演 09-08 那次崩：那次是 action_sets.xml 逐字拷贝 Native → 同 soln id 合并出
	     **重复定义** → KeyNotFoundException。本次 module_sounds.xml 的 48 个音效名与 Native 的
	     **零交集**（Native 实际只定义 3 条 example 音效）→ 合并是纯追加，不冲突。
	     回退方法：删掉下面这一行即可。 -->
	<file id="soln_module_sound" name="ModuleData/module_sounds.xml" type="module_sound"/>
	<!-- 🔴 2026-09-16 启用第四行：soln_item_holsters → ModuleData/item_holsters.xml（火器背后斜挂）。
	     为什么必须开：item_holsters 与 skins / module_sounds 同属 project.mbproj 的 soln 体系，
	     **不在 SubModule 的 XmlNode 体系里**——不开这行 = 那个文件完全不加载，铁炮继续用
	     Native 的 `crossbow_back` 槽 = 枪管朝天（实机症状：枪托卡在背上沿、枪管直挺挺朝上）。
	     引擎侧合并函数 = MBObjectManager.GetMergedXmlForNative("soln_item_holsters")
	     （TaleWorlds.MountAndBlade.CreateProcessedItemHolstersXMLForNative，已反编译确认），
	     把**所有模块**注册到该 soln id 的文件合并后原样交给 native。
	     为什么不会重演 09-08 那次崩：那次是 action_sets.xml 逐字拷贝 Native → 同 soln id 合并出
	     **重复定义** → KeyNotFoundException。本次 item_holsters.xml 只加了两个**新 id**
	     （taikou_firearm / taikou_firearm_2），与 Native 的 id 零交集 → 合并是纯追加，不冲突。
	     （该合并**没有** action_sets 那样的同名去重，是原样直送 native，所以新 id 是硬要求。）
	     回退方法：删掉下面这一行即可（item_holsters.xml 留着不生效，无害；
	     但物品侧的 item_holsters="taikou_firearm..." 会查不到槽 → 需一并回退物品那两行）。 -->
	<file id="soln_item_holsters" name="ModuleData/item_holsters.xml" type="item_holster"/>
	<!-- 🔴 2026-09-19 启用第五、六行：soln_action_sets / soln_action_types（铁炮装填动画接线）。
	     为什么必须开：这两个文件与 skins / module_sounds / item_holsters 同属 soln 体系，
	     **不在 SubModule 的 XmlNode 体系里**——不开这两行 = ModuleData/action_{sets,types}.xml
	     **从不解析** = act_gun_reload 静默不存在（实机症状：`custom.do_anim act_gun_reload` 报 act_none、
	     铁炮装填照旧播引擎的弩动作）。
	     为什么不会重演 09-08 那次崩：那次是 action_sets.xml **逐字拷贝 Native** → 同 soln id 合并出
	     **重复定义** → KeyNotFoundException。本次两个文件合计只加**一个全新动作**（act_gun_reload）
	     与**一条绑定**，与 Native 的动作名零交集 → 合并是纯追加，不冲突。
	     （引擎侧合并函数 = Module.CreateProcessedActionSetsXMLForNative，已反编译确认：
	       同 id 的 action_set 是由 C# 层【追加合并】进第一个，原版几千个动作不会被挤掉。）
	     回退方法：删掉下面这两行即可（action_*.xml 留着不生效，无害）。 -->
	<file id="soln_action_sets" name="ModuleData/action_sets.xml" type="action_set"/>
	<file id="soln_action_types" name="ModuleData/action_types.xml" type="action_type"/>
	<!-- 🔴 2026-09-19 启用第七行：soln_item_usage_sets —— 这一行是【探针】，不是功能。
	     目的 = 验证"内容包注册 soln_item_usage_sets 之后，引擎到底读不读它"
	     （背景与判据详见 ModuleData/item_usage_sets.xml 头部注释）。
	     安全性：那个文件此刻是**空根** → 即使被合并进去也等于什么都没加，不会崩。
	     回退方法：删掉下面这一行即可。 -->
	<file id="soln_item_usage_sets" name="ModuleData/item_usage_sets.xml" type="item_usage_set"/>
	<!-- 🔴 2026-09-23 启用第八行：soln_particle_systems → ModuleData/particle_systems_yinmo.xml
	     （阴魔斩的拖尾 / 命中爆散 / 蓄力三个粒子效果，15 个 emitter）。
	     🔴🔴 **2026-09-24 实机更正（重要，别再按下面那段理解）**：这一行**只让编辑器认出这份 XML**，
	        **游戏运行期读不到它** —— 运行期的粒子表来自 `AssetPackages` 里的 `Particle` 资产（编译产物）。
	        判据：`tpaccli dump` 按名字捞原版 `psys_game_burning_jar_trail` → 打出的是 `META Particle`；
	        反证：HikageRising 一个粒子 XML 都没有、122 个粒子全是 tpac 资产且确实生效。
	        ⇒ 本文件 + 这一行的身份 = **编辑器源**。要进游戏还得在编辑器里 Publish 出粒子包、
	          产物改名拷进 `Taikou/AssetPackages/`；完整五步见 plans/法术体系-通用施法框架.md §B。
	     （以下是 2026-09-23 当时写的设计说明，保留备查：）
	     为什么必须开：粒子与 skins / module_sounds / item_holsters 同属 project.mbproj 的 soln 体系，
	     **不在 SubModule 的 XmlNode 体系里**——不开这行 = 这个文件完全不加载 = 法术的拖尾与爆散
	     **一个都看不见**（`ParticleSystemManager.GetRuntimeIdByName` 查不到，[Spell] 日志会打"粒子查不到"）。
	     为什么不会重演 09-08 那次崩（同 soln id 合并出重复定义 → KeyNotFoundException）：
	     11 个 effect 名（lwn_yinmo_charge / _trail / _burst）与 Native 的 218 个**零交集**，
	     与 TaikouAnim 沙箱里那份是同一份文件（那边是给 ModKit 预览用的，运行时不要勾选 TaikouAnim）
	     ⇒ 合并是纯追加。
	     ⚠️ 本文件是工具链生成物的**交付副本**：生成器 = tools/particle-pipeline/gen_particle_effect.py
	        （spec = examples/yinmo_spec.py），产物先落 out/yinmo_slash.xml，再拷到本模块并改名为
	        particle_systems_yinmo.xml（名字照 Native 惯例：particle_systems_*.xml）。
	        **重生成后必须重新拷一次**，两份不会自动同步（铁律 22：生成物禁手改）。
	     回退方法：删掉下面这一行即可（文件留着不生效，无害）。 -->
	<file id="soln_particle_systems" name="ModuleData/particle_systems_yinmo.xml" type="particle_system"/>
</base>
